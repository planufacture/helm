apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "planufacture.fullname" . }}-test-connection"
  labels:
    {{- include "planufacture.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: "{{ .Values.busybox.image.repository }}:{{ .Values.busybox.image.tag }}"
      command: ['wget']
      args: ['{{ include "planufacture.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
